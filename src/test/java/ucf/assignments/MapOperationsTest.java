package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MapOperationsTest {

    //create a HashMap<String,List<Item>>
    //create some items and a lists. populate list with items.

    @Test
    void addList() {
        //user the add list function to add the list to the map.
        //assert that the list is in the map.
    }

    @Test
    void deleteList() {
        //use the delete list function to remove the list from the map.
        //assert that the list is no longer in the map.
    }
}